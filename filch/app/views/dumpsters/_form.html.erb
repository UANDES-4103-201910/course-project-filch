<%= form_with(model: dumpster, local: true) do |form| %>
  <% if dumpster.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dumpster.errors.count, "error") %> prohibited this dumpster from being saved:</h2>

      <ul>
      <% dumpster.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :rant_id %>
    <%= form.text_field :rant_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
